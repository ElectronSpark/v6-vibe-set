build-*/
build/
*.swp
*.swo
.vscode/
.idea/
compile_commands.json

# Sub-repositories (toolchain/, kernel/, user/, ports/) are recorded as
# gitlinks pointing at their current commit SHA. Promote to true submodules
# (with .gitmodules) once remote URLs are available — see
# scripts/setup-submodules.sh and .gitmodules.template.

# Generated / scratch
/musl-xv6/
*.log

